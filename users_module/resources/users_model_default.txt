from django.contrib.auth.models import AbstractUser
from django.db import models

# Uncomment and edit the following User model to represent your needs. The following has been extended for future
# ease as recommended by
# https://docs.djangoproject.com/en/3.1/topics/auth/customizing/#using-a-custom-user-model-when-starting-a-project

class User(AbstractUser):
    pass
